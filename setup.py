import sys

# Deprecated: This repository no longer uses setup.py for installation.
# Please use the Makefile targets or run scripts directly from the scripts/ directory.
# This stub exits intentionally to avoid accidental installs against an outdated layout.

sys.exit(
    "setup.py is deprecated and no longer supported.\n"
    "Use the Makefile or direct script invocation (python scripts/<script>.py) instead."
)
